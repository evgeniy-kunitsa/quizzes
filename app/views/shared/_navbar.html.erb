<nav class="navbar navbar-toggleable-md navbar-inverse bg-primary">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="#">Survey</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="<%= is_active?(surveys_path) %>">
        <%= link_to surveys_path, class: 'nav-link' do %>
          All surveys
          <span class='badge badge-info'> <%= surveys_count %> </span>
        <% end -%>
      </li>
      <li class="<%= is_active?(new_survey_path) %> dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          New survey
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <%= link_to 'Opinion Poll', new_survey_path(type: 'poll'), class: 'dropdown-item' %>
          <%= link_to 'Quiz', new_survey_path(type: 'quiz'), class: 'dropdown-item' %>
          <%= link_to 'Score Survey', new_survey_path(type: 'score'), class: 'dropdown-item' %>
        </div>
      </li>
    </ul>
    <% if current_user %>
      <span class="navbar-text">
        Logged in as:
        <b> <%= current_user.name %> </b>
        <%= link_to '(change name)', '#', 'data-toggle': 'modal', 'data-target': '#submit-name-modal' %>
      </span>
    <% end %>
  </div>
</nav>
